# Additional requirements for the legacy TensorFlow application
# (core/model.py, cli/, web/ — inference with models/CRNN/best_model_V3.h5).
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-tf.txt

tensorflow==2.10.0
protobuf<3.20

# Web interface and audio acquisition
streamlit>=1.22.0,<1.23.0
pytube>=15.0.0,<16.0.0
