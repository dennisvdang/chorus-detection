# Requirements for the chorus detection pipeline and web app
# (preprocessing, training, inference, streamlit UI). Python >= 3.8.
#
# The original TensorFlow implementation lives on the `tensorflow` branch.

# Core scientific stack (pins chosen for Python 3.8 compatibility)
numpy>=1.24.0,<1.25.0
scipy>=1.10.0,<1.11.0
scikit-learn>=1.3.0,<1.4.0
pandas>=2.0.0,<2.1.0
matplotlib>=3.7.0,<3.8.0
tqdm>=4.65.0

# PyTorch
torch>=2.0.0
pyyaml>=6.0.0

# Audio processing
librosa>=0.10.0,<0.11.0
soundfile>=0.12.1,<0.13.0
pydub>=0.25.1,<0.26.0
ffmpeg-python>=0.2.0,<0.3.0

# Web interface and audio acquisition
streamlit>=1.22.0,<1.23.0
pytube>=15.0.0,<16.0.0

# Install the package itself for development
-e .
